// IF STATEMENT
// - Default for boolean is true
/**
 * if(condition) {
 * console.log();
 * }
 */

let isOn = true;

if (isOn == true) {
    console.log( 'The light is on!')
};

if (isOn) {
    console.log('The light is on! Yay!')
};

let isOff = true;

if (isOff == false) {
    console.log('The light is still on.')
};

if (isOff) {
    console.log('The light is finally off.')
};

let weather = 65

if(weather<70){
    console.log('Wear a jacket')
};