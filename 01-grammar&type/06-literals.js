/**
 * Literals- things that are hard-coded (not variables)
 * JavaScript syntax defines two types of values: Fixed and variable values.
 * Fixed values are called Literals.
 * Variable values are called Variables.
 */

 let days = ['Monday', 'Tuesdsay', 'Wednesday', 'Thursday', 'Friday'];

 console.log(days);  //[ 'Monday', 'Tuesdsay', 'Wednesday', 'Thursday', 'Friday' ]
 console.log(days.toString());  //Monday,Tuesdsay,Wednesday,Thursday,Friday
 console.log(days.length);  //5

 let tired = true;

 let soup = {
     a: 'chicken noodle',
     b: 'tomato',
     c: 'beef and barley'
 };

 console.log(soup.a);  //chicken noodle = "dot notation"
 console.log(soup.c);

 let car = 'Honda';

 let dec = 10.2;
 